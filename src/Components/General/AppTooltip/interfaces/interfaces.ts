import { TooltipProps } from 'antd';
import { ColorState } from '../../../../Constants/Colors';

export type AppTooltipProps = TooltipProps;

export interface StyledTooltipProps {
	tooltiptheme?: ColorState;
}
